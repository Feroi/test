<nav class="navbar navbar-expand-lg bg-blue mb-4">
  <div class="container-fluid">
    <a class="navbar-brand" href="./">Demo</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon text-light">
        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-justify" viewbox="0 0 16 16">
          <path fill-rule="evenodd" d="M2 12.5a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5zm0-3a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5zm0-3a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5zm0-3a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5z"/>
        </svg>
      </span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="/../api">Api Rest</a>
          </li>
          <li class="nav-item">
            <a class="nav-link border-light" href="{{ path('app_product_index') }}">Front View</a>
          </li>
          <li class="nav-item">
            <a class="nav-link border border-light" href="{{ path('app_logout') }}">Logout</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link border border-light" href="{{ path('app_login') }}">Login</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>

{% if app.user %}
  <div class="container d-flex justify-content-end mb-4">
    You are logged in as :<span class="text-lowercase text-primary">&nbsp;{{app.user.email|capitalize }}</span>
  </div>
{% endif %}